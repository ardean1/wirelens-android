# Security policy

WireLens Phone is a **local** per-app network **usage** monitor (`NetworkStatsManager` + Usage Access). It does not use `VpnService` for normal monitoring, does not decrypt HTTPS, and is not antivirus.

Report vulnerabilities via GitHub private reporting on the published repo when available.
